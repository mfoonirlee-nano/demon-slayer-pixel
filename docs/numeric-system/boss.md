# Boss 数值

> 实现状态：已实现。本文记录当前源码中已经生效的 Boss 数值和行为。

## Purpose

记录当前 Boss 出场、阶段、AI 行为、召唤、投射物、专属技能和血量曲线。完整目标设计见 [boss-archetypes.md](boss-archetypes.md)。

## Current State

当前已接入基础 Boss 注册表和 `bossKills` 状态。v1 可玩顺序只启用蛛弦、枯铃，之后循环；完整 13 幕 Boss 系统暂不进入运行时。Boss 死亡奖励和重生计时统一由 `defeatBoss()` 处理。

美术与设定文案见 [../art/bosses/spider-string.md](../art/bosses/spider-string.md) 和 [../art/bosses/dead-bell.md](../art/bosses/dead-bell.md)。本文只维护当前源码已经生效的 Boss 数值和行为。

## Key Formulas

出场（蛛弦基准；枯铃读取注册表覆盖的生命曲线、碰撞伤害、AI 冷却和绘制表）：

| 项 | 当前值 |
| --- | ---: |
| 初始 Boss 计时器 | `28s` |
| 最早允许时间 | `elapsed > 18s` |
| 实际首个 Boss 出场 | 通常约 `28s` |
| 生成 X | `WIDTH + 140` |
| 目标 X | `WIDTH - 170 = 790` |
| 入场速度 | `-2.6` |
| 入场后 AI 延迟 | `32` 帧 |
| 蛛弦碰撞体 | `104 x 188` |
| 蛛弦生命 | `460 + elapsed * 2.2` |
| 枯铃碰撞体 | `104 x 188` |
| 枯铃生命 | `520 + bossKills * 60 + elapsed * 1.45` |

如果 Boss 在 `28s` 生成，生命约为 `521.6`。

当前注册表：

| Boss | id | 原案解锁幕 | 运行时状态 |
| --- | --- | ---: | --- |
| 蛛弦 | `spider-string` | 1 | 阶段 1 突进近战、阶段 2 蛛网投射物、阶段 3 地下蛛丝柱、蚀醒千丝牢笼 |
| 枯铃 | `dead-bell` | 6 | 声波环、横向音刃、停拍组合窗口 |

蛛弦阶段技能累计解锁，进入新阶段后仍保留此前招式：

| 阶段 | 条件 | 实际接触伤害 | 累计技能 |
| --- | --- | ---: | --- |
| 1 | 初始 | `21` | 前摇、突进、近战普攻、后退与喘息 |
| 2 | 生命低于 `66%` | `24` | 阶段 1 全部动作 + `白蛛穿针` |
| 3 | 生命低于 `33%` | `27` | 阶段 1、2 全部动作 + 地下蛛丝柱 |

接触伤害：

```ts
bossAttackDamage(12 + phase * 2)
```

蛛弦移动与 AI：

| 行为 | 当前规则 |
| --- | --- |
| 冲刺前摇 | `max(18, 24 - (phase - 1) * 2)` 帧；基础阶段依次为 `24 / 22 / 20` 帧，结束时锁定冲刺方向 |
| 冲刺窗口 | `26` 帧，速度为 `6 + phase`；触及场地边缘时提前结束 |
| 近战普攻 | 冲刺结束后停步攻击 `36` 帧；第 `18-24` 帧为命中窗口，每次动作最多命中一次 |
| 近战命中体 | `120 x 112`，向面朝方向前伸；伤害为 `Boss 接触伤害 x 1.25` |
| 后退窗口 | `28` 帧，速度为 `3.2`，始终远离玩家 |
| 喘息窗口 | `48` 帧，保持静止且不开始新攻击 |

阶段 2 解锁蛛弦 Boss 技能 1：

| 项 | 当前值 |
| --- | ---: |
| 初始冷却 | `150` 帧 |
| 后续冷却 | `260` 帧 |
| 施法时长 | `54` 帧 |
| 特效生成时间 | 第 `28` 帧 |
| 特效速度 | `16` |
| 特效重力 | `0.45` |
| 特效伤害 | `Boss 接触伤害 x 2` |
| 玩家命中冷却 | `24` 帧 |
| 敌人命中冷却 | `18` 帧 |
| 最低阶段 | `2` |

各阶段技能伤害：

| 阶段 | 伤害 |
| --- | ---: |
| 2 | `48` |
| 3 | `54` |

阶段 3 解锁地下蛛丝柱；进入阶段 3 后，柱阵与 `白蛛穿针` 交替施放：

| 项 | 当前值 |
| --- | ---: |
| 施法时长 | `54` 帧 |
| 特效生成时间 | 第 `30` 帧 |
| 后续冷却 | `300` 帧 |
| 柱数 | `7` |
| 柱间距 | `108` |
| 错峰间隔 | 每远离中心一位增加 `5` 帧 |
| 每柱预警 | `24` 帧 |
| 每柱可见时长 | `30` 帧（另有 `24` 帧预警） |
| 特效帧时长 | `5` 帧 |
| 命中体 | `54 x 228` |
| 单柱伤害 | `bossAttackDamage(14 + phase * 3)` |
| 每柱命中限制 | 最多命中玩家一次 |

蛛弦蚀醒大招 `千丝牢笼`：

| 项 | 当前值 |
| --- | ---: |
| 触发条件 | `awakened && phase >= 3` |
| 首次触发 | 进入可行动的 phase 3 后优先触发 |
| 后续冷却 | `1200` 帧 |
| 总施法时长 | `234` 帧 |
| 分段 | 地面段、空中段、混合段 |
| 安全列 | 全场 `5` 列中保留 `1` 列 |
| 首段预警 | `42` 帧 |
| 后两段预警 | `36` 帧 |
| 每段命中 | `14` 帧 |
| 前两段间隔 | `18` 帧 |
| 末段恢复 | `42` 帧 |
| 伤害 | `Boss 接触伤害 x 1.4` |
| 蛛丝减速 | `54` 帧，横向移动倍率 `0.55` |
| 结束后 AI 延迟 | `90` 帧 |

蚀醒大招伤害：

| 阶段 | 伤害 |
| --- | ---: |
| 3 | `25.2` |
| 4 | `28` |

枯铃阶段：

| 阶段 | 条件 | 接触伤害 | AI 冷却 |
| --- | --- | ---: | ---: |
| 1 | 初始 | `12` | `100` 帧 |
| 2 | 生命低于 `66%` | `14` | `88` 帧 |
| 3 | 生命低于 `33%` | `16` | `76` 帧 |

枯铃声波与音刃：

| 项 | 当前值 |
| --- | ---: |
| 初始冷却 | `120` 帧 |
| 单次施法时长 | `64` 帧 |
| 组合施法时长 | `86` 帧 |
| 单次生成时间 | 第 `30` 帧 |
| 组合生成时间 | 第 `34` 帧 |
| 后续冷却 | `max(160, 238 - phase * 18)` 帧 |
| 组合冷却 | `288` 帧 |
| 反击硬直 | `46` 帧 |
| 声波预警 | `24` 帧 |
| 声波扩散 | `76` 帧 |
| 声波半径 | `54 -> 390` |
| 声波厚度 | `34` |
| 二段声波延迟 | `34` 帧 |
| 音刃预警 | `20` 帧 |
| 音刃命中体 | `238 x 34` |
| 音刃速度 | `7.4` |
| 音刃寿命 | `120` 帧 |
| 伤害 | 声波 `11 + phase * 3`，音刃再 `+2` |

枯铃技能节奏：

| 阶段 | 规则 |
| --- | --- |
| 1 | 稳定释放单圈 `枯铃震`，先预警内圈，再扩散声波环 |
| 2 | 声波变为延迟二段，并向玩家所在高度穿插一枚 `裂音刃` |
| 3 | `停拍索命`：停拍提示后释放声波、上层音刃、延迟声波和下层音刃，随后进入可反击硬直 |

Boss 死亡后：

- 玩家得分增加 `220`。
- 技能能量增加 `60`。
- 大招能量增加 `40`。
- `state.bossKills += 1`。
- `state.boss = null`。
- `state.bossSpawnTimer = PLAYER_COMBAT.skillChargeResetDelay`，当前实际作为 `45s` 重生延迟使用。

当前未使用的 Boss 绘制偏移常量：

- `BOSS_CONFIG.drawOffsetX`
- `BOSS_CONFIG.drawOffsetY`
- `BOSS_SKILL1_CONFIG.drawOffsetX`
- `BOSS_SKILL1_CONFIG.drawOffsetY`

## Code Sources

- `src/constants/combat.ts`
- `src/constants/assets.ts`
- `src/constants/runtime.ts`
- `src/entities/boss.ts`
- `src/entities/player.ts`
- `src/entities/particle.ts`
- `src/entities/projectile.ts`
- `src/runtime.ts`

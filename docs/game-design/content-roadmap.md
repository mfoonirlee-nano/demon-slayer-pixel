# 内容扩展优先级

> 实现状态：设计文档。本文只安排内容和系统落地顺序，不表示新增内容已经接入。

## Priority Overview

| 优先级 | 内容 | 验证目标 |
| --- | --- | --- |
| P0 | 统一当前实现状态、验收指标和原案入口 | 文档不会把目标设计写成当前功能 |
| P1 | `defeatBoss()`、`bossKills`、`act`、`threatScalar`、基础配置入口 | Boss 击杀只结算一次，幕数可稳定派生 |
| P2 | 敌人按幕生成池、Boss 注册表、平台/奖励读取幕数 | 第 1-4 幕有明显体验差异 |
| P3 | XP 升级三选一、Boss 装备三选一、选择 overlay | 单局成长闭环成立 |
| P4 | 新 Boss、新敌人、BGM/SFX、UI polish | 内容池能支撑长期轮换 |

## Enemy Content

| 敌人 | 当前状态 | 玩法目的 | 后续需求 | 优先级 |
| --- | --- | --- | --- | --- |
| `chaser` | 已接入基础追踪 archetype | 基础移动与普攻压力 | 目标设计中改为更稳定的横穿/重入场规则 | P2 |
| `crawler` | 已接入低伏前扑状态机 | 低位穿插，迫使玩家注意脚边 | 调整按幕权重，确保普攻可稳定命中 | P2 |
| `runner` | 已接入前摇冲刺状态机 | 速度压力，迫使跳跃或技能处理 | 第 2 幕后提高权重，控制同时冲刺数量 | P2 |
| `duelist` | 已接入近战前摇斩击状态机 | 近身窗口和反打节奏 | 第 2 幕核心敌人，完善权重和召唤池 | P2 |
| `brute` | 已接入重型 brace/stomp 状态机 | 高血阻挡和空间压迫 | 第 3 幕核心敌人，控制同屏数量 | P2 |
| `caster` | 已接入远程鬼火状态机 | 远程牵制和站位压力 | 第 3 幕核心敌人，限制投射物密度 | P2 |
| `binder` | 已接入控场咒圈状态机，当前按 90s 时间解锁 | 后期控场与反风筝 | 改为第 6 幕+ 或轮换 profile 解锁 | P3 |
| `leaper` | 未实现 | 跳跃落点压力 | 需要正式素材、前摇、落点标记和 active cap | P4 |
| `glider` | 未实现 | 低空与平台边缘压力 | 需要正式素材、俯冲前摇和空中碰撞规范 | P4 |
| `burrower` | 未实现 | 潜行包抄 | 需要地面轨迹、钻出标记和不可重叠规则 | P4 |
| `splitter` | 未实现 | 清场顺序压力 | 需要分裂体配置、奖励限制和预算规则 | P4 |
| `warden` | 未实现 | 支援核心和目标优先级 | 需要光环可读性和同场唯一限制 | P4 |

## Boss Content

| Boss | 当前状态 | 玩法定位 | 后续需求 | 优先级 |
| --- | --- | --- | --- | --- |
| `lower_moon_spider_string` | 当前 Boss 素材和玩法已接入，设定名未完整接入 HUD | 追猎 + 召唤 + 单向蛛网技 | 迁入 Boss 注册表，补登场提示、击败提示和阶段视觉增强 | P1 |
| `lower_moon_mist_bone` | 未实现 | 区域封锁 + 骨刺弹幕 | 正式素材、地面危险区、延迟爆发读法 | P4 |
| `lower_moon_mirror_dream` | 未实现 | 分身干扰 + 反射投射物 | 分身规则、真假可读性、反射物安全窗口 | P4 |
| `lower_moon_fang_gale` | 未实现 | 高速冲刺 + 近身连击 | 冲刺前摇、刹车恢复、连击上限 | P4 |
| `lower_moon_lantern_ember` | 未实现 | 召唤强化 + 火线封路 | 召唤物联动、火线区域、Boss 与小怪 cap | P4 |
| `lower_moon_dead_bell` | 未实现 | 节奏压迫 + 组合弹幕 | 停拍窗口、声波环和节奏提示 | P4 |
| `grand_boss_blood_moon_many_faces` | 未实现，终盘特殊挑战 | 多机制换相 | 不进入普通轮换池；每阶段只启用一个主特性和一个副特性 | P4 |

## Skill Content

当前三招和大招已经满足第一版战斗闭环：

| 技能 | 当前定位 | 后续设计需求 |
| --- | --- | --- |
| 水龙破 | 前方飞行水龙，路径伤害 | 进入 `SKILL_DEFS`，支持升级奖励读取伤害倍率 |
| 打潮刃 | 前方水流剑气，短距离高覆盖 | 进入 `SKILL_DEFS`，明确命中框和 UI 说明 |
| 静水返 | 防护水幕，受击反击 | 明确抵挡次数、反击目标、与 Boss 技能交互 |
| 大招 | 大范围清场/打 Boss 爆发 | 明确获得频率、释放无敌或硬直边界 |

第一版不需要新增技能。先把现有三招配置化，避免 XP 和装备系统接入时把倍率写散。

## Map and Reward Content

| 内容 | 当前状态 | 后续需求 | 优先级 |
| --- | --- | --- | --- |
| 片段生成 | 已实现 8 种片段、张力、奖励预算、低层恢复 | 接入按幕权重和 `threatScalar` | P2 |
| 平台速度 | 当前按 `elapsed` 线性增长 | 改为 `elapsed + bossKills` 混合增长 | P2 |
| 水晶 | 攻击/治疗水晶已实现 | 按幕调整数值和风险路线收益 | P3 |
| 宝箱 | 攻击/治疗宝箱已实现 | 与装备系统区分，避免 Boss 掉落价值被稀释 | P3 |
| XP | 未实现 | 击杀奖励统一 helper 后接入 | P3 |
| 装备 | 未实现 | Boss 击杀后选择，替换同槽装备 | P3 |

## Music and Audio Content

音乐方向已经有完整 prompt，但 runtime 当前只有简单 tone SFX。接入顺序建议：

1. Start Screen、Act 1、Spider String 三首先验证音色。
2. 补 Boss intro sting、victory、game over、low health warning。
3. 再生成 Act 2、Act 3、Act 4+ 和其他 Boss 主题。
4. 所有 BGM 导入前先确认 loop seam，无爆音和断点。

音频实现前不要把 prompt 文档当作已接入素材。

## First Buildable Milestone

最小可交付目标不是一次性做完所有内容，而是先完成“Boss 击杀推进第 2 幕”的闭环：

1. 当前 Boss 击杀统一入口。
2. 增加 `bossKills` 和 `act` helper。
3. 平台、敌人和 Boss 重生读取 `act`，但先不接新素材。
4. HUD 显示当前幕。
5. 击败 `蛛弦` 后进入第 2 幕，敌人权重和平台权重发生可感知变化。

这个里程碑通过后，再接 XP 和装备 overlay。
